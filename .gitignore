# Dependencies — Go (if vendored)
vendor/

# Legacy Node artifacts — kept for reference after TS→Go migration 2026-08-25
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Build outputs — Go plugin + legacy TS
dist/
build/
out/
.next/
.nuxt/
.coverage/
coverage/
.nyc_output/
*.tsbuildinfo
*.so
/backend.so

# Environment — never commit real secrets
.env
.env.local
.env.*.local

# Local data / Docker volumes — top-level only, keep nakama config
/data/
pgdata/

# Nakama local data — keep local.yml, ignore runtime artifacts
nakama/data/*.db
nakama/data/*.log
nakama/data/.cookie
nakama/data/modules/
nakama/data/*.tmp

# Logs
logs/
*.log

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*.swo
*~

# Test / temp
tmp/
temp/
