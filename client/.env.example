# Client env — never commit real secrets, copy to .env for local dev
# Backend at 127.0.0.1:7350 (docker compose up --build -d from repo root)
VITE_NAKAMA_HOST=127.0.0.1
VITE_NAKAMA_PORT=7350
VITE_NAKAMA_KEY=defaultkey
VITE_NAKAMA_USE_SSL=false
# For Vite dev server
VITE_DEV_PORT=5173
